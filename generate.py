
import random
#import random
# we can use different functions from the random library here like shuffle() randomint() and 
#coin = choice(["heads" , "tails"])
#print(coin)
cards = ["queen", "king", "joker", "spades", "ikaa"]

card = random.shuffle(cards)
for card in cards:
    print(card)


# python also have statistics library for the projets
